[package]
name = "paper-native-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
name = "paper_native_core"
path = "src/lib.rs"

[dependencies]
lz4 = "1.28.1"
sha2 = { version = "=0.10.9", default-features = false, features = ["asm"] }
