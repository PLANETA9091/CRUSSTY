[package]
name = "paper-native-jni"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
name = "paper_native_jni"
crate-type = ["cdylib"]
path = "src/lib.rs"

[dependencies]
jni = "0.19.0"
paper-native-core = { path = "../paper-native-core" }
