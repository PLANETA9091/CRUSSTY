[package]
name = "hello"
version = "0.1.0"
edition = "2021"
description = "Proof c-plugin: class-load hook + GetLoadedClasses + JNI Bukkit.getLogger().info"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
cplug-abi = { path = "../../cplug-abi" }
cplug-sdk = { path = "../../cplug-sdk" }
