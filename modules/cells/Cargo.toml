[package]
name = "cells"
version = "0.1.0"
edition = "2021"
description = "c-cells — the server as a simulated living organism: human HL2/3 neuron dynamics (na/kv/NMDA, Cm=0.5) drive adaptive ticking and target method re-optimization"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
cplug-abi = { path = "../../cplug-abi" }
cplug-sdk = { path = "../../cplug-sdk" }