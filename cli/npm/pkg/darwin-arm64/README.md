# crussty-darwin-arm64

Platform binary for [crussty](https://www.npmjs.com/package/crussty) (darwin-arm64).

Do not install this package directly — it is pulled in automatically by the `crussty` package based on your platform:

```bash
npm i -g crussty
```

Source: https://github.com/PLANETA9091/CRUSSTY
